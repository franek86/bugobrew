# bugobrew

> Bugobrew is an independent craft brewery.

## Build Setup

```bash
# install dependencies
Bugobrew
$ npm install

backend
$ npm install

frontend
$ npm install

# run strapi backend and nuxt frontend
Bugobrew
$ npm run dev


```
